<Window x:Class="OnlineCourseApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Online Course Platform" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <!-- Головна сітка -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <!-- Навбар -->
            <ColumnDefinition Width="*"/>
            <!-- Контент -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Хедер -->
            <RowDefinition Height="*"/>
            <!-- Основна частина -->
        </Grid.RowDefinitions>

        <!-- Хедер -->
        <Border Grid.ColumnSpan="2" Grid.Row="0" Background="#ffffff" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Назва платформи -->
                <TextBlock Text="OnlineCourseApp" FontWeight="Bold" FontSize="20" VerticalAlignment="Center"/>

                <!-- Кнопка "Увійти" справа -->
                <Button x:Name="LoginButton" Content="Увійти" Grid.Column="1" Padding="10 5" Margin="0 0 10 0"
                Background="#3498DB" Foreground="White" FontWeight="SemiBold"
                HorizontalAlignment="Right" VerticalAlignment="Center"
                Cursor="Hand" Click="LoginButton_Click"/>

                <!-- Логін юзера справа -->
                <Label x:Name="UsernameLabel"
                HorizontalAlignment="Right"
                Visibility="Collapsed"
                FontWeight="Bold"
                FontSize="14"
                Margin="10,0,0,0"/>

            </Grid>
        </Border>

        <!-- Навігаційна панель -->
        <StackPanel Grid.Row="1" Grid.Column="0" Background="#2C3E50" Margin="20">
            <TextBlock Text="Головна" Foreground="White" Margin="0 10" FontSize="16"/>
            <TextBlock Text="Курси" Foreground="White" Margin="0 10" FontSize="16"/>
            <TextBlock Text="Мій Профіль" Foreground="White" Margin="0 10" FontSize="16"/>
            <TextBlock Text="Налаштування" Foreground="White" Margin="0 10" FontSize="16"/>
            <Button x:Name="AuthorPanelButton" Content="Панель автора" HorizontalAlignment="Left" VerticalAlignment="Top" Background="{x:Null}" FontSize="16" BorderBrush="{x:Null}" Foreground="White" Click="AuthorPanelButton_Click" Visibility="Collapsed"/>
            <Button x:Name="AdminPanelButton" Content="Панель адміна" HorizontalAlignment="Left" VerticalAlignment="Top" Background="{x:Null}" FontSize="16" BorderBrush="{x:Null}" Foreground="White" Click="AdminPanelButton_Click" Visibility="Collapsed"/>


        </StackPanel>

        <!-- Контент -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" Padding="20">
            <StackPanel>
                <TextBlock Text="Рекомендовані Курси" FontSize="22" FontWeight="Bold" Margin="0 0 0 20"/>

                <ItemsControl>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <!-- Мокапи курсів -->
                    <Border Background="White" Width="200" Height="150" Margin="10" Padding="10" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Курс C#" FontWeight="Bold"/>
                            <TextBlock Text="Автор: Іван" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="4.5 ★" Foreground="Orange" FontSize="12"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" Width="200" Height="150" Margin="10" Padding="10" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="WPF для початківців" FontWeight="Bold"/>
                            <TextBlock Text="Автор: Оля" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="4.7 ★" Foreground="Orange" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </ItemsControl>
                <TextBlock FontSize="22" FontWeight="Bold"><Run Language="uk-ua" Text="Інші"/><Run Text=" Курси"/></TextBlock>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Courses}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Margin="10" Padding="10" Width="200" Background="White">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="Автор:" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Author}" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding Rating, StringFormat='Рейтинг: {0:F1}'}" Foreground="OrangeRed"/>
                                        <Button Content="Записатися"
                                Margin="0,10,0,0"
                                Command="{Binding DataContext.EnrollCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>